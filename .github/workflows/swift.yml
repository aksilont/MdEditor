name: MdEditor

on:
  push:
    branches: 
      - develop

jobs:
  build:
    name: Build MdEditor
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jdx/rtx-action@v1
      - name: Build
        run: exec ./.github/build.sh